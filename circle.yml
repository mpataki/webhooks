test:
  override:
    - echo 'no tests'

deployment:
  tag_version:
    branch: circle_test
    commands:
      - git config user.email "matpataki@gmail.com"
      - git config user.name "mpataki"
      - git tag -a $(cat VERSION)
      - git push origin --tags
